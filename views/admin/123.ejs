<div class="d-flex justify-content-between align-items-center row pt-4 pb-2 gap-4 gap-md-0 gx-5">
    <div class="col-md-4">
        <label for="dateRange">Custom Date Range:</label>
        <input type="text" id="dateRange" class="form-control" placeholder="Select date range" />
    </div>
    <div class="col-md-4">
        <button class="btn btn-primary" id="filterBtn">Filter</button>
    </div>
    <div class="col-md-4">
        <button class="btn btn-secondary" id="lastDayBtn">Last 1 Day</button>
        <button class="btn btn-secondary" id="lastWeekBtn">Last 1 Week</button>
        <button class="btn btn-secondary" id="lastMonthBtn">Last 1 Month</button>
    </div>
</div>













const response = await fetch(form.action, {
    method: 'POST',
    body: formData
});

const text = await response.text(); // Get the response as text
console.log(text); // Log the response text

let result;
try {
    result = JSON.parse(text); // Try to parse it as JSON
} catch (error) {
    console.error("Failed to parse JSON:", error);
    // Handle the error, e.g., show an alert
    Swal.fire({
        icon: 'error',
        title: 'Error!',
        text: 'An unexpected error occurred.',
        confirmButtonText: 'OK'
    });
    return;
}